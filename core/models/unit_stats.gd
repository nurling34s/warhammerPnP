## Immutable, shared unit definition (a Godot Resource, cached/shared by the engine).
## Base fields common to both rulesets; AoSUnitStats/FortyKUnitStats add their own
## ruleset-specific fields on top. Filled in during the placeholder-data milestone.
class_name UnitStats
extends Resource
